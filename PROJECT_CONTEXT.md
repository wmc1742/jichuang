# 即创设计 AI 项目上下文

## 目标

把即创从单一商业视频生成工具升级为类似 Codex 的通用创作 Agent。原型用于验证三类核心场景：

1. 首页：发起任务、选择商品或素材、选择创作能力。
2. 会话：呈现用户输入、Agent 思考、流式回复、阶段产物和确认动作。
3. 内容：在工作台查看、编辑、切换和确认图片、视频、分析报告、创意脚本、分镜与预览素材。

近期测试重点是会话消息的展示、AI 产出内容的呈现，以及展开后的各类产物详情。

## 典型用户故事

用户经营 3C 电器，销售 Labubu 联名款自带线充电宝，需要为双十一制作一批推广素材。

商品信息：

- Labubu 联名外观、官方授权
- 大容量、体积小巧、支持快充
- 原价 299 元，双十一到手价 199 元
- 赠送 Labubu 小盲盒

用户有明确商品和促销目标，但创意方向模糊。他需要选择商品、输入需求，让 Agent 完成需求分析、创意与分镜、素材预览和最终成片。

## 主流程

1. 用户进入首页，新建项目默认选中，名称为 `新的项目`，工作台不展开。
2. 用户点击输入框加号，在商品库中选择 Labubu 充电宝并确认。
3. 商品带入输入框，用户输入双十一推广诉求并发送。
4. 项目名称在发送后更新为 Labubu 项目名称，会话显示用户消息和商品附件。
5. Agent 先显示单独的临时思考状态。
6. 思考提示消失，Agent 流式回复已理解诉求并说明下一步。
7. 原位出现“成片需求拟定中”阶段卡片，默认可收起。
8. 用户可点击展开卡片，查看节奏自然、速度不过快的流式分析过程。
9. 生成完成后，同一张卡片原位切换成“成片需求已生成”，可直接在会话中查看最终内容。
10. 用户通过自然语言、编辑或确认按钮确认需求分析。
11. Agent 再次经历思考、回复和阶段卡片状态，生成三条创意脚本与分镜方案。
12. 用户在会话中选择方案，也可打开右侧工作台切换、查看和编辑方案。
13. 进入预览环节，为每个分镜生成视频素材。用户总览、选择或替换不合适的画面。
14. 用户确认素材，Agent 合成最终成片。

## 消息与阶段状态

这些是替换关系，不是多张相同阶段卡片依次堆叠：

1. `thinking`：独立且短暂的思考提示。
2. `assistant-stream`：思考结束后的自然语言流式回复。
3. `artifact-generating-collapsed`：阶段产物生成中、收起状态。
4. `artifact-generating-expanded`：同一卡片展开并流式展示过程。
5. `artifact-completed`：生成完成后在原卡片位置替换展示。

创意脚本、预览和成片沿用同一状态机，只替换标题、图标、内容数据和产物详情组件。

## 页面框架

整体由三部分组成：

- 项目：项目管理、全局功能、项目列表。
- 会话：顶部导航、消息内容区、输入操作区。
- 工作台：顶部导航、切换 Tab、产物内容区。

会话内容宽度与底部确认卡、输入框宽度不同。确认卡和输入框同宽，并比普通消息内容区向两侧略宽。工作台默认关闭，仅在打开阶段产物或用户主动点击时展开。

## 组件化方向

- `Button`：图标按钮、图标加文字、纯文字、主要/次要/幽灵/禁用状态。
- `Icon`：由语义名称映射具体 SVG，可按阶段替换。
- `Media`：图片、视频、封面、加载、选中、替换、失败和预览状态。
- `Composer`：首页输入、会话输入、确认附着卡、商品与素材附件。
- `Message`：用户消息、Agent 回复、思考、阶段产物和系统反馈。
- `ArtifactCard`：生成中收起、生成中展开、已生成。
- `Workbench`：分析、创意、分镜、预览、图片、视频和最终成片详情。
- `Tabs/SegmentedControl`：创意 1/2/3、内容类型和产物切换。

Mock 数据应与组件解耦，使商品、图片、视频、文案、阶段耗时和模型输出都能单独替换。

## 视觉与交互原则

- 以 Figma 最新稿为准，目标接近 100% 还原。
- 不使用 Unicode 字符冒充图标，必须使用设计稿内对应图标资源。
- 所有可见行动点都应支持点击、切换、滚动、Hover、选中和必要的编辑。
- 流式输出应有真实等待感；不要一次性快速显示全文。
- 图片和视频必须保持清晰，兼容 MacBook Pro Retina 与至少 2K 显示器。
- 不把整张 Figma 截图塞进输入框、卡片或工作台充当实现。
- 默认状态、尺寸、圆角、间距和状态替换逻辑与单个页面的视觉同等重要。

## 设计优先级

1. 最新架构和分类方案：<https://www.figma.com/design/Alh06MJP5p6N9cZkuHD5I4/AI-native%E6%8E%A2%E7%B4%A2?node-id=565-7960&t=wxo0yv9KN8LFanle-1>
2. 页面框架、组件说明和编号 Demo：<https://www.figma.com/design/8qoF27fJET70e7zl6dL6RB/%E5%8D%B3%E5%88%9BAgent-2026Q3?node-id=143-5166>

发生差异时先遵循最新架构稿；对于最新稿未覆盖的消息状态、阶段图标和完整流程，再参考 Q3 Demo 对应画板。

## 技术方向

- Demo 优先保持轻量、可在线访问和跨电脑继续。
- 交互状态、组件、Mock 数据和媒体资源应相互解耦。
- 真实模型通过本地或云端服务代理接入，前端不保存 API 密钥。
- 后续优先规范消息组件、阶段状态机、工作台产物详情和媒体组件。