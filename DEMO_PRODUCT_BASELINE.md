# Agent 2.0 产品流程基线

版本：20260906a。会话视觉基线继续沿用已确认的「会话 1.0」。

## 本轮校准来源

Figma 文件：`Alh06MJP5p6N9cZkuHD5I4`。
第一部分：`1176:118745`，1·整体信息架构。

| 设计节点 | 含义 | 实现对应 |
| --- | --- | --- |
| 1047:46495 | 任务、会话、产物三栏，可展开/收起 | templates/workspace.js、components/navigation.js |
| 1047:43885 | 新建入口、技能推荐及 hover 说明 | components/task-dialogs.js、product.css |
| 1047:44028 / 1047:44147 | 任务栏收起及恢复 | TaskSidebar |
| 1047:44266 | 选择技能后输入需求 | Composer、selectedSkill |
| 1047:44411 | 添加图片、视频、文本 | TaskDialogs、tasks/media-store.js |
| 1047:44564 / 1047:44749 | 技能选择与全部技能入口 | SkillChoices、技能选择对话框 |
| 1047:44888 | 商品库选择 / 本地上传 | 商品选择对话框、IndexedDB |
| 1047:46404 | 产物空状态 | ArtifactOverview |

读取以上设计上下文后，对旧实现做了代码核查：旧任务列表固定、新建预装产物、修改正文不保存、自由输入无条件跳到下一阶段。这些旧行为不再作为新要求的依据。

本轮是功能连通基线，不等于逐像素验收完成。商品库目前仅放置一个螺蛳粉样例；技能列表与选择能力已接通，但多行业技能的独立业务数据尚未补齐。技能弹窗、筛选细节和复杂分镜内容仍需随着下一轮完整数据一起校准。

## 数据与逻辑

- Task 独立拥有 messages、artifacts、product、settings、draft、questionDrafts、pendingRun 和产物工作区状态。
- 任务保存到 localStorage；上传的图片 / 视频文件保存到 IndexedDB，刷新后恢复 URL。
- 新任务初始 messages / artifacts 为空；历史示例任务和新任务分开存储。
- `conversation/workflow.js` 决定业务分支；`conversation/runtime.js` 更新消息状态；组件注册表决定展示组件。
- 思考和执行是同一个 Run 的不同状态；只有 Tool 开始事件才进入执行。
- 追问提交：原追问 answered/收起 → 用户气泡“我已确认” → 下一轮 Run。
- 当前存在未回答追问时，不因普通输入而跳过确认。输入保留，用户可以继续完成追问。
- 接受分镜可以直接进入成片确认；请求更换演员才走候选演员分支。
- 产物实体独立存储，消息只用 artifactId / artifactIds 展示；发布函数按实际输出加入产物，去重且不覆盖已编辑版本。
- 编辑使用独立 draft；应用保存正文 / 描述 / 画面及 revision/history；取消不覆盖已保存的实体。
- 引用携带 artifactId；引用后的文字反馈记录到该产物。尚未接入根据自由反馈重新生成的真实模型。
- 分享生成压缩的只读任务快照；本地上传素材暂不支持跨设备分享，不伪造可分享状态。

## 已验证

- Node：28 个测试通过，包含原会话状态机测试、产物注册表测试，以及任务隔离、持久化、分支、产物按时发布、编辑版本测试。
- 浏览器：从新建开始，手动输入需求、选择跨年节点、提交追问、完成需求与分镜、直接进入最终成片确认并生成五条视频条目。
- 用户选择能影响确认摘要、后续回复和需求正文。
- 编辑需求正文并应用，刷新后正文仍为保存后的版本。
- 手机 390px 视口，任务栏可展开，主流程和视频详情没有页面横向溢出。
- 20 秒视频实际播放，readyState=4、currentTime 正常推进。
- 停止任务后刷新保留恢复入口；上传图片刷新后从 IndexedDB 恢复。
- 分享快照保留消息，并禁用输入；从快照新建任务清除快照状态。
- 点击“继续生成”可恢复停止的任务并到达下一追问。视频画面草稿切换画面后仍保留，应用后 revision 更新，引用后返回会话携带正确产物附件。
- 页面可视化编辑入口仍可打开，点击 Run 能查看 Mock 来源、状态事件及待接入 API 契约。
- 测试中未发现浏览器控制台 error；测试页面图片无加载失败。

## 当前能力边界

1. 没有真实 AI、Skill、Tool、视频生成算法或计费 API。当前实现的是可替换的规则驱动 Mock。
2. 多条视频目前共用一份 20 秒静音素材预演，片内带 DEMO 标识；不是五个不同的真实生成结果。源脚本在 scripts/generate-demo-film.swift。
3. 视频编辑会保存画面与包装配置，不会将自然语言修改真正渲染进视频文件。
4. 演员口播试听调用浏览器系统语音，不是演员专属模型音色。
5. GitHub Pages 不提供账号后台、跨用户实时同步、线上直接改仓库或上传文件托管。可视化编辑在网页端保留本地预览；本地编辑服务器可保存配置到源码。
6. 其他技能的独立场景、商品详情、促销真实价格、完整演员资料、每条视频差异、失败重试策略和后端接口，属于下一轮数据与服务接入范围。

## 下一轮数据包

按同一任务提供：商品事实与图片 → 投放节点与活动规则 → 需求分析 → 创意分镜 → 主体/演员 → 画面片段 → 预览方案 → 最终视频。每个产物带稳定 ID、类型、版本、来源关系和对应的媒体 URL；消息只引用这些 ID，不再复制一套产物实体。

## 后续改动规则

- 新 Figma / 标注是设计与交互依据；旧代码仅在核对后复用。
- 实施前列出设计节点、状态、逻辑、组件、数据字段的对应关系。
- 不用增加临时样式来掩盖数据或时序错误。
- 新增消息状态必须同时定义：触发事件、原位更新或新增、完成状态、展开历史、组件映射。
- 不把 Mock 契约说成已经接通的真实 API，也不把保存编辑配置说成完成了媒体再生成。
- 验收分开记录：样式、交互、时序、持久化、媒体、跨设备。未验证项必须明确写出。
