# Codex 协作约定

## 开始工作前

1. 先读取 `PRODUCT_CONTRACT.md`，它定义当前产品结构、可复用边界和验收条件；再读取 `PROJECT_CONTEXT.md`。
2. 读取 `CONVERSATION_HISTORY.md`。
3. 查看 README 中的两个 Figma 链接。
4. 以最新 Figma 和已确认的用户规则为视觉、结构和流程来源。Q3 Demo 和下述早期状态描述仅是历史记录，不允许作为缺失设计的补充依据。

## 实现原则

- 不 invent 设计稿中不存在的消息、卡片、按钮或过程内容。
- 不使用 Unicode 字符冒充图标，优先使用 Figma 导出的 SVG。
- 图标通过语义映射替换，按钮支持有图标和无图标变体。
- 图片、视频、文本和阶段耗时使用可替换数据驱动。
- Agent 思考、流式回复、产物生成中和产物已生成是连续替换状态。
- 已生成状态必须替换原来的生成中卡片，不能同时存在。
- 工作台默认关闭；打开产物详情时再展开。
- 可见行动点必须具备点击、切换、滚动、Hover、选择、编辑或明确禁用状态。
- 不把完整设计稿截图当作页面组件。
- 保持 Retina 和 2K 显示器上的清晰度，不使用全局 transform 缩放页面。
- 真实模型通过服务端代理接入，浏览器中不得保存 API 密钥。

## 组件边界

- 状态和流程逻辑与展示组件分离。
- Mock 数据与组件分离。
- 图片和视频复用统一媒体组件。
- 消息中的产物展示引用独立产物对象；按照产物类型选择文稿卡、图片、形象或视频展示，不能统一塞进 ArtifactCard。
- 输入框持有 Skill、预设 query、素材槽、引用和用户文字；不能把 Skill 移到输入框外。
- 文稿按结构化内容块渲染，不能按产物 ID 或标题临时拼页面。
- 未校准的新要求不得套用旧代码；先检查相关代码是否符合当前规则，再决定保留或替换。
- 同类按钮和图标通过变体配置，不复制大段页面结构。

## 验证要求

完成修改后至少验证：

- 首页默认状态和工作台关闭状态。
- 商品选择、输入和发送。
- 项目首次发送后的改名。
- 思考、流式回复、产物生成和完成替换。
- 产物详情工作台的打开、关闭、切换和滚动。
- 浏览器无资源缺失和 JavaScript 错误。
- 参考视口和一个较窄桌面视口中无文本重叠或异常裁切。

## 记录规则

每次形成新的关键设计决策时，更新 `CONVERSATION_HISTORY.md`。涉及产品流程、组件边界或设计优先级的稳定结论，同时更新 `PROJECT_CONTEXT.md`。
